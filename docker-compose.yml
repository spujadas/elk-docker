# Docker Compose file for ELK stack

# Build with:
# docker-compose build fn-elk

# Run with:
# docker-compose up

fn-elk:
  build: .
  ports:
    - "5601:5601"
    - "9200:9200"
    - "9300:9300"
    - "9000:9000"
